# Лабораторная работа №5: Итераторы и аллокаторы

## 📋 Описание

Реализация контейнера **Queue (очередь)** с кастомным аллокатором на основе фиксированного блока памяти.

**Автор:** bakholdinivan  
**Дата:** 2024-11-16  
**Вариант:** Очередь + Фиксированный блок памяти + std::map

## ✅ Выполненные требования

| Требование | Статус |
|-----------|--------|
| Наследник `std::pmr::memory_resource` | ✅ |
| Фиксированный блок (выделяется один раз) | ✅ |
| **Информация о блоках в `std::map`** | ✅ |
| Переиспользование освобождённой памяти | ✅ |
| Автоматическая очистка при уничтожении | ✅ |
| Шаблонный контейнер Queue | ✅ |
| Использование `std::pmr::polymorphic_allocator` | ✅ |
| Forward итератор (`std::forward_iterator_tag`) | ✅ |
| Тесты с простыми типами (int) | ✅ |
| Тесты со сложными типами (struct) | ✅ |
| Правило пяти | ✅ |

## 🚀 Сборка и запуск

### Требования

- C++17
- CMake 3.10+
- Google Test (скачивается автоматически)

### Сборка

```bash
mkdir build && cd build
cmake ..
cmake --build .
```

### Запуск демонстрации

```bash
./lab05_demo
```

### Запуск тестов

```bash
./lab05_tests
```

Или через CTest:

```bash
ctest --output-on-failure
```

## 📂 Структура проекта

```
lab05/
├── src/
│   ├── fixed_memory_resource.hpp    # Аллокатор (объявление)
│   ├── fixed_memory_resource.cpp    # Аллокатор (реализация)
│   └── queue.hpp                    # Контейнер очередь
├── tests/
│   └── test_lab05.cpp              # Google Tests (40+ тестов)
├── main.cpp                         # Демонстрация
├── CMakeLists.txt                  # Система сборки
└── README.md                       # Документация
```

## 🧪 Тесты Google Test

**Всего: 40+ тестов**

### FixedMemoryResource (7 тестов)
- Выделение памяти
- Освобождение памяти
- Переиспользование памяти
- Обработка переполнения
- Некорректное освобождение
- Конструктор перемещения
- Оператор присваивания перемещением

### Queue<int> (15 тестов)
- Пустая очередь
- push и size
- front и back
- pop
- Исключения (pop, front на пустой)
- clear
- Конструктор копирования
- Конструктор перемещения
- Операторы присваивания
- Итераторы (begin/end)
- Range-based for
- Модификация через итератор
- Постфиксный инкремент
- Сравнение итераторов

### Queue<Person> (4 теста)
- push и front для struct
- Семантика перемещения
- Оператор -> итератора
- Несколько объектов

### Интеграционные тесты (2 теста)
- Циклические push/pop
- Концепция Forward Iterator

## 🔍 Пример использования

```cpp
#include "fixed_memory_resource.hpp"
#include "queue.hpp"

int main() {
    FixedMemoryResource memory(4096);
    Queue<int> queue(&memory);
    
    queue.push(10);
    queue.push(20);
    queue.push(30);
    
    for (const auto& val : queue) {
        std::cout << val << " ";
    }
    
    queue.pop();
    std::cout << queue.front();
    
    return 0;
}
```

## 📊 Сложность операций

| Операция | Сложность |
|----------|-----------|
| push() | O(1) |
| pop() | O(1) |
| front() / back() | O(1) |
| size() / empty() | O(1) |
| Итератор (обход) | O(n) |

## 🎓 Ключевые особенности

### Почему Queue в .hpp, а FixedMemoryResource разделён?

- **Queue** — шаблонный класс → вся реализация в .hpp
- **FixedMemoryResource** — обычный класс → можно разделить на .hpp/.cpp

### Как работает переиспользование?

```
1. pop() → блок добавляется в free_blocks_
2. push() → сначала проверяются free_blocks_
3. Если подходящий блок найден → переиспользуем ♻️
```

### Правило пяти

Реализовано для корректного управления ресурсами:
- Деструктор
- Конструктор копирования
- Оператор присваивания копированием
- Конструктор перемещения
- Оператор присваивания перемещением

## 📝 Лицензия

Образовательный проект. MIT License.

---

**Удачи на защите! 🚀**